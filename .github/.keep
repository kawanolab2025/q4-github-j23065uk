GitHubのアカウント作成し、ローカル環境にGitインストールPersoal access tokenを作成すれば準備完了。
リポジトリとはプロジェクト単位のデータ管理場所。ローカルやリモート上で管理する。
リモートリポジトリとはGitHub上に置かれているリポジトリでほかの開発者と共有できます。ブランチ運用の機能とはブランチ作成既存のコードに影響を与えず、新しい作業、（機能追加や修正）を行うために分岐を作る。ブランチ切り替え複数の作業を並行して進めるため、必要に応じて作業対象のブランチを変更する。プッシュ作成したブランチをリモートリポジトリにアップロードして共有する。プルリクエストブランチの作業内容をレビュー・確認して、メインブランチ（mainなど）へ統合するための依頼。マージプルリクエストを通して、作業ブランチの変更をmainなどに統合する操作。コンフリクト解消異なるブランチ間で同じファイルを変更している場合に、手動で競合を解決してマージする。
GitHubでのプルからコミットまでの流れまず、作業を始める前に、リモートリポジトリの最新状態を取得します。GitHubにアクセスし、ローカルリポジトリを開きます。git pull origin mainコマンドを実行しリモートのmainブランチから最新の変更をローカルに反映します。ファイルを編集し、変更をリモートに反映させる準備として、変更内容をステージングします。変更をローカルリポジトリに保存するために、コミットを作成し、最後にプッシュコマンドを実行して、変更をリモートにアップロードします。ローカル変更をリモートリポジトリに反映させます。
コンフリクトとは、Gitが自動的にマージできない変更が発生した状態を指します。具体的には、異なるブランチで同じファイルの同じ部分を異なる方法で変更した場合に発生します。Gitは、どちらの変更を採用すべきか自動的に判断できないため、手動で解決しなければなりません。
feature-branchで以下のように変更：
html
<h1>新しい見出し</h1>

mainブランチで以下のように変更：
html
<h1>旧タイトル</h1>
このような場合、git mergeを実行するとコンフリクトが発生します。
